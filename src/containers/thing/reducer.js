import * as actions from '../../constants/actions';

const init_state = {
    otherValue: 'red'
};

const things = (state = init_state, action) => {
    switch (action.type) {
        default:
        return state;
    }
}

export default things;