import * as actions from '../../constants/actions';

const init_state = {
    otherValue: 'red'
};

const home = (state = init_state, action) => {
    switch (action.type) {
        default:
        return state;
    }
}

export default home;