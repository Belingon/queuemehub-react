import * as actions from '../../constants/actions';

const init_state = {
    value: 'blue'
};

const app = (state = init_state, action) => {
    switch (action.type) {
        default:
        return state;
    }
}

export default app;